#include <iostream>

using namespace std;

class Calcualtor
{
  public:
    int Calculate(int, int, char);
};

int main()
{
    int x, y, result;
    cout << "Hello! I'm calculator!" << endl;
    cin.ignore();
    cin.get();
    return 0;
    cin >> x >> y >> oper;
    Calcualtor c;
    results = c.Calculate(x, y, oper);
};
